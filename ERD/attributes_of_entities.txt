-- ΑΓΓΕΛΙΑ

aggelia_id (primary)
akinhto_id (foreign)
pwlhths_id (->melos_id, foreign)
price
aggelia_type (select)
created_on (date)
modified_on (date)
closed_on (date)
available (bool)
available_since (date)
text (text)

-- ΑΚΙΝΗΤΟ

akinhto_id (primary)
surface_area
area (varchar)
area_coords
description (text)
extra (text)

-- A. ΚΑΤΟΙΚΙΑ

akinito_id (foreign-primary)
katoikia_type (select)
heating_system (select)
bathrooms
floor
parking_spot (bool)
construnction_year (date)
internal (text)
external (text)

-- A. ΕΠΑΓ. ΧΩΡΟΣ

akinito_id (foreign-primary)
parking_spot (bool)
construnction_year (date)
internal (text)
external (text)

-- A. ΓΗ

akinito_id (foreign-primary)
building_coeff
external(text)

-- ΜΕΛΟΣ

melos_id (primary)
username (unique)
password (varchar)
email (varchar)
first_name (varchar)
last_name (varchar)
is_endiaferomenos (bool)
is_pwlhths (bool)

-- Μ. ΕΝΔΙΑΦΕΡΟΜΕΝΟΣ

melos_id (foreign-primary)

-- Μ. ΕΝΔΙΑΦΕΡΟΜΕΝΟΣ_ΕΝΔΙΑΦΕΡΕΤΑΙ

melos_id (foreign-primary)
aggelia_id (foreign-primary)

-- Μ. ΠΩΛΗΤΗΣ

melos_id (foreign-primary)
AFM (varchar-unique)
telephone (varchar)
is_mesiths (bool)

-- ΜΕΣΙΤΗΣ

AFM (foreign-primary)
brand_name (varchar)
brand_address (varchar)

-- ΙΔΙΟΚΤΗΤΗΣ

AFM (primary-varchar)
first_name (varchar)
last_name (varchar)

-- ΙΔΙΟΚΤΗΤΗΣ_ΕΧΕΙ

akinito_id (foreign-primary)
idiokthths_afm (->AFM, foreign-primary)
